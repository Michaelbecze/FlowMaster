[project]
name = "flowmaster-shared"
version = "0.1.0"
description = "Cross-service event and contract types for the FlowMaster enterprise platform"
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.6",
    "fastapi>=0.115",
    "httpx>=0.27",
]

[tool.setuptools.packages.find]
where = ["src"]
