# Kubernetes manifests

Structure-only placeholder (plan.md Project Structure). Production-oriented manifests for
the six backend services, `gateway`, and `frontend` are filled in during the Phase 7
hardening pass (see `specs/001-enterprise-netflow-platform/tasks.md`, task T091). Local
development uses `infra/docker-compose.yml` instead.
